# actgpr

Active GPR (Gaussian Process Regression) Optimisation package.

## Commands

run gitignored test : `poetry run python run_test.py`

run objective tests : `poetry run pytest -v tests/unit/test_objective.py`

run surrogate tests : `poetry run pytest tests/unit/test_surrogate.py`

